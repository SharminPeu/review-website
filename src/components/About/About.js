import React from 'react';
import MenuBar from '../MenuBar/MenuBar';

const About = () => {
    return (
        <div>
            <MenuBar></MenuBar>
            <h2>This is about</h2>
        </div>
    );
};

export default About;