import React from 'react';
import Header from '../Header/Header';
import MenuBar from '../MenuBar/MenuBar';
// import Header from '../Header/Header';

const Home = () => {
    return (
        <div>
            <MenuBar></MenuBar>
            <h2>This is home</h2>
        </div>
    );
};

export default Home;